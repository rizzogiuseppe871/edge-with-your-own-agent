# 🤖 Reasonix Edge 浏览器自动化系统\n\n> 让您的 Reasonix Agent 完全控制 Edge 浏览器，实现智能化前端自动化\n\n## ⚡ 快速概览\n\n这个项目让 Reasonix Agent 可以：\n\n✅ **自动化网页操作** - 填表、登录、导航、点击\n✅ **执行任意脚本** - 在网页上下文中运行 JavaScript\n✅ **智能信息提取** - 自动获取文本、视频、图片、链接\n✅ **媒体控制** - 调整视频倍速、下载文件\n✅ **内容翻译** - 支持 7+ 种语言翻译\n✅ **页面监控** - 实时监控页面变化\n✅ **AI 驱动** - Reasonix 自主分析和决策\n\n---\n\n## 🏗️ 系统架构\n\n```\n┌──────────────────────────────┐\n│   Reasonix Agent             │  ← 您的本地 AI Agent\n│   (思考、分析、决策)          │\n└───────────────┬──────────────┘\n                │ HTTP/WebSocket\n                ▼\n┌──────────────────────────────┐\n│  Reasonix Proxy Server       │  ← 中间层（localhost:9999）\n│  (转发命令、管理连接)        │\n└───────────────┬──────────────┘\n                │\n                ▼\n┌──────────────────────────────┐\n│   Edge Extension             │  ← 浏览器扩展\n│   ├─ popup.js                │\n│   ├─ content.js              │\n│   ├─ background.js           │\n│   └─ reasonix-agent.js       │  ← 新增\n└───────────────┬──────────────┘\n                │\n                ▼\n        ┌──────────────┐\n        │   网页 DOM   │  ← 实际操作发生的地方\n        │   JavaScript │\n        └──────────────┘\n```\n\n---\n\n## 🚀 5 分钟快速开始\n\n### 步骤 1：启动 Reasonix Proxy（1 分钟）\n\n**选项 A：自动启动**\n\n```bash\n# macOS/Linux\ncd reasonix-proxy\n./start.sh\n\n# Windows\ncd reasonix-proxy\nstart.bat\n```\n\n**选项 B：手动启动**\n\n```bash\ncd reasonix-proxy\nnpm install\nnode server.js\n```\n\n✅ 看到这个消息表示成功：\n```\n🚀 Reasonix Proxy Server 已启动\n📍 监听地址: http://localhost:9999\n```\n\n### 步骤 2：在 Edge 中加载扩展（1 分钟）\n\n1. 打开 `edge://extensions/`\n2. 右上角启用「开发者模式」\n3. 点击「加载未打包的扩展程序」\n4. 选择项目文件夹\n\n✅ 看到扩展已加载，Proxy 输出显示：\n```\n✅ 扩展已连接: 1234567890\n```\n\n### 步骤 3：配置 Reasonix（1 分钟）\n\n告诉您的 Reasonix Agent 使用这个 Proxy：\n\n```python\n# 在 Reasonix 配置中添加\nconfig = {\n    \"edge_extension\": {\n        \"enabled\": True,\n        \"proxy_url\": \"http://localhost:9999\",\n        \"timeout\": 5000\n    }\n}\n```\n\n或设置环境变量：\n```bash\nexport REASONIX_EDGE_PROXY=http://localhost:9999\n```\n\n### 步骤 4：测试连接（1 分钟）\n\n```python\n# 在 Reasonix 中运行\nresponse = reasonix.edge.execute({\n    \"command\": \"get_status\"\n})\n\nprint(response)  # 应该显示: {'status': 'ready', ...}\n```\n\n### 步骤 5：开始使用（1 分钟）\n\n```python\n# 获取页面内容\ncontent = reasonix.edge.execute({\n    \"command\": \"get_page_content\"\n})\n\nprint(content[\"result\"][\"text\"])  # 页面的所有文本\n```\n\n✅ **完成！您现在可以用 Reasonix 控制 Edge 浏览器了！**\n\n---\n\n## 📚 核心功能详解\n\n### 1. 获取页面内容\n\n```python\nresponse = reasonix.edge.execute({\n    \"command\": \"get_page_content\"\n})\n\nresult = response[\"result\"]\nprint(result[\"text\"])          # 页面文本\nprint(result[\"title\"])         # 页面标题\nprint(result[\"meta\"])          # meta 标签\n```\n\n### 2. 执行 JavaScript 脚本\n\n```python\n# 简单脚本\nresponse = reasonix.edge.execute({\n    \"command\": \"execute_script\",\n    \"params\": {\n        \"code\": \"document.title\"\n    }\n})\n\nprint(response[\"result\"][\"result\"])  # 页面标题\n\n# 复杂脚本\nresponse = reasonix.edge.execute({\n    \"command\": \"execute_script\",\n    \"params\": {\n        \"code\": \"\"\"\n            Array.from(document.querySelectorAll('a'))\n                .map(a => ({\n                    text: a.innerText,\n                    href: a.href\n                }))\n        \"\"\"\n    }\n})\n\nlinks = response[\"result\"][\"result\"]\nprint(f\"找到 {len(links)} 个链接\")\n```\n\n### 3. 修改 DOM 元素\n\n```python\n# 修改文本\nreasonnix.edge.execute({\n    \"command\": \"modify_dom\",\n    \"params\": {\n        \"selector\": \"h1\",\n        \"action\": \"text\",\n        \"value\": \"新标题\"\n    }\n})\n\n# 修改 HTML\nreasonix.edge.execute({\n    \"command\": \"modify_dom\",\n    \"params\": {\n        \"selector\": \"#content\",\n        \"action\": \"html\",\n        \"value\": \"<p>新内容</p>\"\n    }\n})\n\n# 修改 CSS 类\nreasonix.edge.execute({\n    \"command\": \"modify_dom\",\n    \"params\": {\n        \"selector\": \".button\",\n        \"action\": \"class\",\n        \"value\": \"btn btn-primary\"\n    }\n})\n\n# 修改样式\nreasonix.edge.execute({\n    \"command\": \"modify_dom\",\n    \"params\": {\n        \"selector\": \"body\",\n        \"action\": \"style\",\n        \"value\": {\"background\": \"red\", \"color\": \"white\"}\n    }\n})\n\n# 修改属性\nreasonix.edge.execute({\n    \"command\": \"modify_dom\",\n    \"params\": {\n        \"selector\": \"input[name='email']\",\n        \"action\": \"attr\",\n        \"value\": {\"name\": \"value\", \"val\": \"user@example.com\"}\n    }\n})\n```\n\n### 4. 翻译文本\n\n```python\nresponse = reasonix.edge.execute({\n    \"command\": \"translate\",\n    \"params\": {\n        \"text\": \"Hello World\",\n        \"target_lang\": \"中文\"\n    }\n})\n\nprint(response[\"result\"][\"translated\"])  # \"你好世界\"\n\n# 支持的语言\n# 中文、英文、日文、韩文、法文、西班牙文、德文、...\n```\n\n### 5. 视频控制\n\n```python\n# 设置播放速度\nreasonix.edge.execute({\n    \"command\": \"video_control\",\n    \"params\": {\n        \"action\": \"set_playback_rate\",\n        \"rate\": 2  # 2 倍速\n    }\n})\n\n# 播放和暂停\nreasonix.edge.execute({\"command\": \"video_control\", \"params\": {\"action\": \"play\"}})\nreasonix.edge.execute({\"command\": \"video_control\", \"params\": {\"action\": \"pause\"}})\n\n# 全屏\nreasonix.edge.execute({\"command\": \"video_control\", \"params\": {\"action\": \"fullscreen\"}})\n\n# 获取视频信息\nresponse = reasonix.edge.execute({\n    \"command\": \"video_control\",\n    \"params\": {\"action\": \"get_info\"}\n})\n\ninfo = response[\"result\"]\nprint(f\"时长: {info['duration']}s, 当前: {info['currentTime']}s\")\n```\n\n### 6. 文件下载\n\n```python\nreasonix.edge.execute({\n    \"command\": \"download_file\",\n    \"params\": {\n        \"url\": \"https://example.com/file.pdf\",\n        \"filename\": \"document.pdf\"  # 可选，如果不指定用原文件名\n    }\n})\n\nprint(\"文件已开始下载\")\n```\n\n### 7. 获取页面信息\n\n```python\nresponse = reasonix.edge.execute({\n    \"command\": \"get_page_info\"\n})\n\ninfo = response[\"result\"]\n\nprint(f\"URL: {info['url']}\")\nprint(f\"标题: {info['title']}\")\nprint(f\"视频数: {len(info['videos'])}\")\nprint(f\"图片数: {len(info['images'])}\")\nprint(f\"链接数: {len(info['links'])}\")\n\n# 获取所有视频\nfor video in info['videos']:\n    print(f\"- {video['title']}: {video['src']}\")\n\n# 获取所有图片\nfor img in info['images']:\n    print(f\"- {img['alt']}: {img['src']}\")\n```\n\n### 8. 隐藏广告\n\n```python\nresponse = reasonix.edge.execute({\n    \"command\": \"hide_ads\"\n})\n\nprint(f\"已隐藏 {response['result']['hidden_count']} 个广告\")\n```\n\n### 9. 获取截图\n\n```python\nresponse = reasonix.edge.execute({\n    \"command\": \"screenshot\"\n})\n\nscreenshot_base64 = response[\"result\"][\"screenshot\"]\n\n# 保存截图\nimport base64\nwith open(\"screenshot.png\", \"wb\") as f:\n    f.write(base64.b64decode(screenshot_base64))\n```\n\n---\n\n## 💡 实际应用示例\n\n### 示例 1：自动爬虫 + AI 分析\n\n```python\nclass WebScraper:\n    def __init__(self):\n        self.edge = reasonix.edge\n    \n    def scrape_and_analyze(self, url):\n        \"\"\"访问网页、提取内容、AI 分析\"\"\"\n        \n        # 1. 获取页面内容\n        response = self.edge.execute({\n            \"command\": \"get_page_content\"\n        })\n        content = response[\"result\"][\"text\"]\n        \n        # 2. 用 Reasonix AI 分析\n        analysis = reasonix.analyze(content)\n        \n        # 3. 翻译关键部分\n        translated = self.edge.execute({\n            \"command\": \"translate\",\n            \"params\": {\n                \"text\": content[:500],\n                \"target_lang\": \"中文\"\n            }\n        })\n        \n        return {\n            \"原始内容\": content[:200],\n            \"翻译\": translated[\"result\"][\"translated\"],\n            \"AI分析\": analysis\n        }\n\nscraper = WebScraper()\nresult = scraper.scrape_and_analyze(\"https://example.com\")\nprint(result)\n```\n\n### 示例 2：批量下载视频\n\n```python\ndef batch_download_videos():\n    \"\"\"自动找到所有视频并下载\"\"\"\n    \n    # 获取页面信息\n    info = reasonix.edge.execute({\n        \"command\": \"get_page_info\"\n    })[\"result\"]\n    \n    videos = info[\"videos\"]\n    print(f\"找到 {len(videos)} 个视频\")\n    \n    # 逐个下载\n    for i, video in enumerate(videos):\n        reasonix.edge.execute({\n            \"command\": \"download_file\",\n            \"params\": {\n                \"url\": video[\"src\"],\n                \"filename\": f\"video_{i}_{video['title']}.mp4\"\n            }\n        })\n        print(f\"✅ 已下载: {video['title']}\")\n\nbatch_download_videos()\n```\n\n### 示例 3：自动表单填充\n\n```python\ndef auto_fill_registration():\n    \"\"\"自动填充注册表单\"\"\"\n    \n    form_data = {\n        \"input[name='username']\": \"myusername\",\n        \"input[name='email']\": \"user@example.com\",\n        \"input[name='password']\": \"securepassword123\",\n        \"select[name='country']\": \"China\"\n    }\n    \n    # 填充每个字段\n    for selector, value in form_data.items():\n        reasonix.edge.execute({\n            \"command\": \"modify_dom\",\n            \"params\": {\n                \"selector\": selector,\n                \"action\": \"attr\",\n                \"value\": {\"name\": \"value\", \"val\": value}\n            }\n        })\n        print(f\"✅ 已填充: {selector}\")\n    \n    # 点击提交按钮\n    reasonix.edge.execute({\n        \"command\": \"execute_script\",\n        \"params\": {\n            \"code\": \"document.querySelector('button[type=submit]').click()\"\n        }\n    })\n    \n    print(\"✅ 表单已提交\")\n\nauto_fill_registration()\n```\n\n### 示例 4：实时页面监控\n\n```python\nimport time\n\ndef monitor_page_updates():\n    \"\"\"定时检查页面变化\"\"\"\n    \n    last_hash = None\n    check_interval = 5  # 每 5 秒检查一次\n    \n    while True:\n        response = reasonix.edge.execute({\n            \"command\": \"get_page_content\"\n        })\n        \n        current_text = response[\"result\"][\"text\"]\n        current_hash = hash(current_text)\n        \n        if current_hash != last_hash:\n            print(\"🔔 页面已更新！\")\n            \n            # 用 AI 分析变化\n            analysis = reasonix.analyze_changes(current_text)\n            print(f\"变化分析: {analysis}\")\n            \n            last_hash = current_hash\n        \n        time.sleep(check_interval)\n\nmonitor_page_updates()\n```\n\n### 示例 5：多步骤自动化工作流\n\n```python\ndef complex_automation_workflow():\n    \"\"\"复杂的多步骤自动化\"\"\"\n    \n    edge = reasonix.edge\n    \n    # 第 1 步：检查登录状态\n    content = edge.execute({\"command\": \"get_page_content\"})[\"result\"]\n    \n    if \"login\" in content[\"text\"].lower():\n        print(\"需要登录，自动登录...\")\n        # 执行登录脚本\n        edge.execute({\n            \"command\": \"execute_script\",\n            \"params\": {\"code\": \"window.location = '/login'\"}\n        })\n        time.sleep(2)\n    \n    # 第 2 步：搜索内容\n    edge.execute({\n        \"command\": \"modify_dom\",\n        \"params\": {\n            \"selector\": \"input[name='search']\",\n            \"action\": \"attr\",\n            \"value\": {\"name\": \"value\", \"val\": \"Python编程\"}\n        }\n    })\n    \n    # 第 3 步：提交搜索\n    edge.execute({\n        \"command\": \"execute_script\",\n        \"params\": {\"code\": \"document.querySelector('form').submit()\"}\n    })\n    \n    time.sleep(2)\n    \n    # 第 4 步：提取结果\n    results = edge.execute({\n        \"command\": \"execute_script\",\n        \"params\": {\n            \"code\": \"\"\"\n                Array.from(document.querySelectorAll('.result-item'))\n                    .slice(0, 10)\n                    .map(item => ({\n                        title: item.querySelector('h2').innerText,\n                        link: item.querySelector('a').href,\n                        description: item.querySelector('.desc').innerText\n                    }))\n            \"\"\"\n        }\n    })[\"result\"][\"result\"]\n    \n    # 第 5 步：处理结果\n    for i, result in enumerate(results):\n        print(f\"{i+1}. {result['title']}\")\n        print(f\"   链接: {result['link']}\")\n        print(f\"   描述: {result['description']}\")\n    \n    return results\n\nresults = complex_automation_workflow()\n```\n\n---\n\n## 🔧 配置和优化\n\n### 自定义超时时间\n\n```python\n# 默认 5 秒，对于复杂页面可能不够\nresponse = reasonix.edge.execute(\n    {\"command\": \"execute_script\", \"params\": {\"code\": \"sleep(3000); return 'done'\"}},\n    timeout=10000  # 10 秒\n)\n```\n\n### 错误处理\n\n```python\ntry:\n    response = reasonix.edge.execute({\n        \"command\": \"get_page_content\"\n    })\n    \n    if response[\"success\"]:\n        content = response[\"result\"]\n        print(content[\"text\"])\n    else:\n        print(f\"错误: {response['error']}\")\n        \nexcept Exception as e:\n    print(f\"请求失败: {e}\")\n```\n\n### 检查 Proxy 状态\n\n```bash\n# 查看连接状态\ncurl http://localhost:9999/api/status\n\n# 查看支持的所有命令\ncurl http://localhost:9999/api/commands\n\n# 查看操作日志\ncurl http://localhost:9999/api/logs\n```\n\n---\n\n## 🐛 故障排除\n\n### 问题 1：\"没有可用的 Edge 扩展连接\"\n\n**解决方案：**\n1. 确保 Proxy 正在运行（`http://localhost:9999` 可访问）\n2. 确保扩展已在 Edge 中加载\n3. 确保至少打开了一个 Edge 标签页\n4. 检查扩展是否启用\n\n### 问题 2：命令执行超时\n\n**解决方案：**\n1. 检查页面是否加载完成\n2. 增加超时时间\n3. 检查网络连接\n4. 简化 JavaScript 代码\n\n### 问题 3：找不到元素\n\n**解决方案：**\n1. 使用浏览器开发工具验证 CSS 选择器\n2. 检查元素是否在 iframe 中\n3. 等待动态内容加载\n4. 使用更具体的选择器\n\n---\n\n## 📊 性能指标\n\n- **响应时间**: < 100ms（网络和页面加载时间除外）\n- **并发连接**: 支持多个 Edge 标签页\n- **脚本执行**: 支持 5s 内的 JavaScript\n- **数据传输**: 无限制（取决于网络）\n\n---\n\n## 🔐 安全性\n\n✅ **本地通信** - 所有命令仅在 localhost 上运行\n✅ **没有数据上传** - 所有操作都在本地浏览器中执行\n✅ **命令白名单** - 只允许预定义的安全命令\n✅ **超时保护** - 防止长时间运行的脚本\n✅ **错误隔离** - 错误不会影响其他操作\n\n---\n\n## 📚 完整文档\n\n- 🎯 [快速开始指南](./docs/REASONIX_QUICKSTART.md)\n- 📖 [API 完整文档](./docs/REASONIX_API.md)\n- 🏗️ [架构设计文档](./REASONIX_INTEGRATION.md)\n- 💻 [项目原始 README](./README.md)\n\n---\n\n## 🤝 贡献\n\n欢迎提交 Issue 和 Pull Request！\n\n---\n\n## 📝 许可证\n\nMIT License\n\n---\n\n## 🙋 支持\n\n- 📖 查看文档\n- 🐛 检查 GitHub Issues\n- 💬 提交新 Issue\n\n---\n\n**现在您拥有一个强大的 AI 驱动浏览器自动化系统！** 🚀\n\n开始构建您的自动化工作流吧！✨\n"